<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.assistanceinformatiquetoulouse.roulezrose.staffeur">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_staffeur"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_staffeur_rounded"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".StaffeurActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.BrandedLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StaffeurDetailActivity"
            android:label="TBD"
            android:parentActivityName=".StaffeurActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>